@page "/animations"

<PageTitle>CSS Animations</PageTitle>

<div class="max-w-7xl mx-auto">
    <h1 class="text-4xl font-bold mb-8">CSS Animations Gallery</h1>

    <div class="space-y-12">
        <!-- Bounce Animations -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Bounce Effects</h2>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                <div class="card bg-base-200 shadow-xl animate-bounce-in">
                    <div class="card-body items-center text-center">
                        <div class="badge badge-primary badge-lg">Bounce In</div>
                        <h3 class="card-title">Entrance</h3>
                        <p>Smooth bounce entrance effect</p>
                    </div>
                </div>
                
                <div class="card bg-base-200 shadow-xl animate-bounce-scale">
                    <div class="card-body items-center text-center">
                        <div class="badge badge-secondary badge-lg">Bounce Scale</div>
                        <h3 class="card-title">Pulsing</h3>
                        <p>Continuous bounce animation</p>
                    </div>
                </div>
                
                <div class="card bg-base-200 shadow-xl hover:animate-bounce-hover">
                    <div class="card-body items-center text-center">
                        <div class="badge badge-accent badge-lg">Hover Me</div>
                        <h3 class="card-title">Interactive</h3>
                        <p>Bounce on hover</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Fade Animations -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Fade Effects</h2>
            <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
                <div class="alert alert-info animate-fade-in">
                    <span>Fade In</span>
                </div>
                <div class="alert alert-success animate-fade-in-up">
                    <span>Fade In Up</span>
                </div>
                <div class="alert alert-warning animate-fade-in-down">
                    <span>Fade In Down</span>
                </div>
                <div class="alert alert-error animate-fade-in-left">
                    <span>Fade In Left</span>
                </div>
            </div>
        </section>

        <!-- Slide Animations -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Slide Effects</h2>
            <div class="flex flex-wrap gap-4">
                <button class="btn btn-primary animate-slide-in-left">Slide Left</button>
                <button class="btn btn-secondary animate-slide-in-right">Slide Right</button>
                <button class="btn btn-accent animate-slide-in-up">Slide Up</button>
                <button class="btn btn-neutral animate-slide-in-down">Slide Down</button>
            </div>
        </section>

        <!-- Rotate & Flip -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Rotate & Flip</h2>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
                <div class="avatar placeholder animate-rotate-in">
                    <div class="bg-primary text-primary-content rounded-full w-24">
                        <span class="text-3xl">R</span>
                    </div>
                </div>
                <div class="avatar placeholder animate-flip-in-x">
                    <div class="bg-secondary text-secondary-content rounded-full w-24">
                        <span class="text-3xl">X</span>
                    </div>
                </div>
                <div class="avatar placeholder animate-flip-in-y">
                    <div class="bg-accent text-accent-content rounded-full w-24">
                        <span class="text-3xl">Y</span>
                    </div>
                </div>
                <div class="avatar placeholder animate-spin-slow">
                    <div class="bg-neutral text-neutral-content rounded-full w-24">
                        <span class="text-3xl">∞</span>
                    </div>
                </div>
            </div>
        </section>

        <!-- Scale & Zoom -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Scale & Zoom</h2>
            <div class="flex flex-wrap gap-6">
                <div class="stat bg-base-200 rounded-box shadow animate-zoom-in">
                    <div class="stat-title">Zoom In</div>
                    <div class="stat-value text-primary">98%</div>
                    <div class="stat-desc">Scale up effect</div>
                </div>
                <div class="stat bg-base-200 rounded-box shadow animate-scale-up">
                    <div class="stat-title">Scale Up</div>
                    <div class="stat-value text-secondary">2.5x</div>
                    <div class="stat-desc">Continuous scale</div>
                </div>
                <div class="stat bg-base-200 rounded-box shadow hover:animate-pulse-scale">
                    <div class="stat-title">Hover Scale</div>
                    <div class="stat-value text-accent">∞</div>
                    <div class="stat-desc">Interactive pulse</div>
                </div>
            </div>
        </section>

        <!-- Shake & Wobble -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Shake & Wobble</h2>
            <div class="flex flex-wrap gap-4">
                <div class="badge badge-lg badge-primary animate-shake">Shake</div>
                <div class="badge badge-lg badge-secondary animate-wobble">Wobble</div>
                <div class="badge badge-lg badge-accent animate-swing">Swing</div>
                <div class="badge badge-lg badge-info animate-jello">Jello</div>
            </div>
        </section>

        <!-- Glow & Pulse -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Glow & Pulse Effects</h2>
            <div class="flex flex-wrap gap-6">
                <button class="btn btn-primary animate-glow">Glow Effect</button>
                <button class="btn btn-secondary animate-pulse-glow">Pulse Glow</button>
                <div class="radial-progress text-primary animate-progress" style="--value:70;">70%</div>
                <div class="loading loading-spinner loading-lg text-accent animate-rainbow"></div>
            </div>
        </section>

        <!-- Text Animations -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Text Effects</h2>
            <div class="space-y-4">
                <h3 class="text-4xl font-bold animate-text-focus-in">Focus In</h3>
                <h3 class="text-4xl font-bold animate-text-blur-out">Blur Out</h3>
                <h3 class="text-4xl font-bold animate-typing">Typing Effect...</h3>
                <h3 class="text-4xl font-bold animate-gradient bg-gradient-to-r from-primary via-secondary to-accent bg-clip-text text-transparent">Gradient Flow</h3>
            </div>
        </section>

        <!-- Complex Animations -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Complex Animations</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div class="card bg-gradient-to-br from-primary to-secondary text-primary-content shadow-2xl animate-float">
                    <div class="card-body">
                        <h3 class="card-title">Floating Card</h3>
                        <p>Smooth floating animation</p>
                        <div class="card-actions justify-end">
                            <button class="btn btn-ghost">Action</button>
                        </div>
                    </div>
                </div>
                
                <div class="card bg-gradient-to-br from-accent to-secondary text-accent-content shadow-2xl animate-tilt">
                    <div class="card-body">
                        <h3 class="card-title">Tilting Card</h3>
                        <p>3D tilt effect</p>
                        <div class="card-actions justify-end">
                            <button class="btn btn-ghost">Action</button>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Loading States -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Loading Animations</h2>
            <div class="flex flex-wrap gap-6 items-center">
                <span class="loading loading-spinner loading-xs"></span>
                <span class="loading loading-spinner loading-sm"></span>
                <span class="loading loading-spinner loading-md"></span>
                <span class="loading loading-spinner loading-lg"></span>
                <span class="loading loading-dots loading-lg text-primary"></span>
                <span class="loading loading-ring loading-lg text-secondary"></span>
                <span class="loading loading-ball loading-lg text-accent"></span>
                <span class="loading loading-bars loading-lg text-info"></span>
                <span class="loading loading-infinity loading-lg text-warning"></span>
            </div>
        </section>

        <!-- Heartbeat & Pulse -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Heartbeat & Pulse</h2>
            <div class="flex flex-wrap gap-6 items-center">
                <div class="btn btn-circle btn-lg btn-error animate-heartbeat">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="currentColor" viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
                </div>
                <div class="avatar placeholder animate-pulse-ring">
                    <div class="bg-primary text-primary-content rounded-full w-24">
                        <span class="text-3xl">📡</span>
                    </div>
                </div>
                <div class="badge badge-lg badge-success animate-ping-badge">Live</div>
            </div>
        </section>

        <!-- Elastic & Rubber Band -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Elastic & Rubber Band</h2>
            <div class="flex flex-wrap gap-6">
                <button class="btn btn-primary animate-elastic">Elastic</button>
                <button class="btn btn-secondary animate-rubber-band">Rubber Band</button>
                <div class="card bg-base-200 shadow-xl w-48 animate-bounce-elastic">
                    <div class="card-body items-center text-center p-4">
                        <span class="text-2xl">🎾</span>
                        <p class="text-sm">Bouncy</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Roll & Rotate -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Roll & Rotate Effects</h2>
            <div class="flex flex-wrap gap-6 items-center">
                <div class="avatar placeholder animate-roll-in">
                    <div class="bg-accent text-accent-content rounded-full w-20">
                        <span class="text-2xl">🎱</span>
                    </div>
                </div>
                <div class="avatar placeholder animate-rotate-scale">
                    <div class="bg-secondary text-secondary-content rounded-full w-20">
                        <span class="text-2xl">⚙️</span>
                    </div>
                </div>
                <div class="badge badge-lg badge-primary animate-flip-horizontal">Flip H</div>
                <div class="badge badge-lg badge-secondary animate-flip-vertical">Flip V</div>
            </div>
        </section>

        <!-- Attention Seekers -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Attention Seekers</h2>
            <div class="flex flex-wrap gap-4">
                <div class="alert alert-warning animate-flash">
                    <span>⚠️ Flash Alert</span>
                </div>
                <button class="btn btn-error animate-tada">Tada!</button>
                <div class="badge badge-lg badge-info animate-head-shake">Head Shake</div>
                <button class="btn btn-success animate-bounce-in-out">Bounce In/Out</button>
            </div>
        </section>

        <!-- Background Animations -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Background Effects</h2>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                <div class="card shadow-xl animate-bg-pan-left">
                    <div class="card-body">
                        <h3 class="card-title">Pan Left</h3>
                        <p>Background panning effect</p>
                    </div>
                </div>
                <div class="card shadow-xl animate-bg-pan-right">
                    <div class="card-body">
                        <h3 class="card-title">Pan Right</h3>
                        <p>Smooth background motion</p>
                    </div>
                </div>
                <div class="card shadow-xl animate-kenburns">
                    <div class="card-body">
                        <h3 class="card-title">Ken Burns</h3>
                        <p>Zoom & pan effect</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Tracking & Spacing -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Text Tracking</h2>
            <div class="space-y-4">
                <h3 class="text-3xl font-bold animate-tracking-in">Tracking In</h3>
                <h3 class="text-3xl font-bold animate-tracking-out">Tracking Out</h3>
                <h3 class="text-3xl font-bold animate-text-pop-up">Pop Up!</h3>
            </div>
        </section>

        <!-- Shadow Effects -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Shadow Animations</h2>
            <div class="flex flex-wrap gap-6">
                <div class="card bg-base-100 shadow-xl w-48 animate-shadow-drop">
                    <div class="card-body items-center text-center p-4">
                        <span class="text-2xl">📦</span>
                        <p class="text-sm">Drop Shadow</p>
                    </div>
                </div>
                <div class="card bg-base-100 shadow-xl w-48 animate-shadow-inset">
                    <div class="card-body items-center text-center p-4">
                        <span class="text-2xl">🔲</span>
                        <p class="text-sm">Inset Shadow</p>
                    </div>
                </div>
                <button class="btn btn-primary animate-shadow-pulse">Pulse Shadow</button>
            </div>
        </section>

        <!-- Back Entrance/Exit -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Back Entrance & Exit</h2>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                <button class="btn btn-primary animate-back-in-up">Back In Up</button>
                <button class="btn btn-secondary animate-back-in-down">Back In Down</button>
                <button class="btn btn-accent animate-back-in-left">Back In Left</button>
                <button class="btn btn-neutral animate-back-in-right">Back In Right</button>
            </div>
        </section>

        <!-- Light Speed -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Light Speed</h2>
            <div class="flex flex-wrap gap-4">
                <div class="badge badge-lg badge-primary animate-light-speed-in-right">Light Speed In</div>
                <div class="badge badge-lg badge-secondary animate-light-speed-in-left">Light Speed In Left</div>
            </div>
        </section>

        <!-- Hinge & Roll Out -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Hinge & Roll Out</h2>
            <div class="flex flex-wrap gap-6">
                <div class="card bg-base-200 shadow-xl w-48 animate-hinge">
                    <div class="card-body items-center text-center p-4">
                        <span class="text-2xl">🚪</span>
                        <p class="text-sm">Hinge</p>
                    </div>
                </div>
                <div class="avatar placeholder animate-roll-out">
                    <div class="bg-primary text-primary-content rounded-full w-20">
                        <span class="text-2xl">🎲</span>
                    </div>
                </div>
            </div>
        </section>

        <!-- Zoom Entrance -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Zoom Variations</h2>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                <div class="alert alert-info animate-zoom-in-up">
                    <span>Zoom In Up</span>
                </div>
                <div class="alert alert-success animate-zoom-in-down">
                    <span>Zoom In Down</span>
                </div>
                <div class="alert alert-warning animate-zoom-in-left">
                    <span>Zoom In Left</span>
                </div>
                <div class="alert alert-error animate-zoom-in-right">
                    <span>Zoom In Right</span>
                </div>
            </div>
        </section>

        <!-- Specials -->
        <section>
            <h2 class="text-2xl font-bold mb-4">Special Effects</h2>
            <div class="flex flex-wrap gap-6">
                <button class="btn btn-primary animate-jack-in-box">Jack In Box</button>
                <div class="avatar placeholder animate-flip">
                    <div class="bg-secondary text-secondary-content rounded-full w-20">
                        <span class="text-2xl">🎴</span>
                    </div>
                </div>
                <button class="btn btn-accent animate-zoom-out-in">Zoom Out In</button>
            </div>
        </section>

        <!-- 3D Card Flip -->
        <section>
            <h2 class="text-2xl font-bold mb-4">3D Card Flip</h2>
            <div class="flex flex-wrap gap-6 justify-center">
                <div class="flip-card">
                    <div class="flip-card-inner">
                        <div class="flip-card-front">
                            <div class="card bg-gradient-to-br from-primary to-secondary text-primary-content shadow-2xl w-64 h-80">
                                <div class="card-body items-center justify-center text-center">
                                    <h3 class="card-title text-3xl mb-4">Front Side</h3>
                                    <p class="text-lg">Hover to flip!</p>
                                    <div class="text-6xl mt-4">🎴</div>
                                </div>
                            </div>
                        </div>
                        <div class="flip-card-back">
                            <div class="card bg-gradient-to-br from-accent to-secondary text-accent-content shadow-2xl w-64 h-80">
                                <div class="card-body items-center justify-center text-center">
                                    <h3 class="card-title text-3xl mb-4">Back Side</h3>
                                    <p class="text-lg">Amazing 3D effect!</p>
                                    <div class="text-6xl mt-4">✨</div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="flip-card">
                    <div class="flip-card-inner">
                        <div class="flip-card-front">
                            <div class="card bg-gradient-to-br from-info to-primary text-info-content shadow-2xl w-64 h-80">
                                <div class="card-body items-center justify-center text-center">
                                    <div class="text-6xl mb-4">📚</div>
                                    <h3 class="card-title text-2xl">Learn More</h3>
                                    <p>Click to reveal</p>
                                </div>
                            </div>
                        </div>
                        <div class="flip-card-back">
                            <div class="card bg-gradient-to-br from-success to-info text-success-content shadow-2xl w-64 h-80">
                                <div class="card-body items-center justify-center text-center">
                                    <div class="text-6xl mb-4">✅</div>
                                    <h3 class="card-title text-2xl">Details</h3>
                                    <p>3D perspective transform with smooth transition</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
</div>

<style>
    /* Bounce Animations */
    @@keyframes bounce-in {
        0% { transform: scale(0); opacity: 0; }
        50% { transform: scale(1.1); }
        100% { transform: scale(1); opacity: 1; }
    }
    
    @@keyframes bounce-scale {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.05); }
    }
    
    .animate-bounce-in {
        animation: bounce-in 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
    }
    
    .animate-bounce-scale {
        animation: bounce-scale 2s ease-in-out infinite;
    }
    
    .animate-bounce-hover:hover {
        animation: bounce-in 0.5s ease;
    }

    /* Fade Animations */
    @@keyframes fade-in {
        from { opacity: 0; }
        to { opacity: 1; }
    }
    
    @@keyframes fade-in-up {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
    }
    
    @@keyframes fade-in-down {
        from { opacity: 0; transform: translateY(-30px); }
        to { opacity: 1; transform: translateY(0); }
    }
    
    @@keyframes fade-in-left {
        from { opacity: 0; transform: translateX(-30px); }
        to { opacity: 1; transform: translateX(0); }
    }
    
    .animate-fade-in { animation: fade-in 1s ease-out; }
    .animate-fade-in-up { animation: fade-in-up 0.8s ease-out; }
    .animate-fade-in-down { animation: fade-in-down 0.8s ease-out; }
    .animate-fade-in-left { animation: fade-in-left 0.8s ease-out; }

    /* Slide Animations */
    @@keyframes slide-in-left {
        from { transform: translateX(-100%); opacity: 0; }
        to { transform: translateX(0); opacity: 1; }
    }
    
    @@keyframes slide-in-right {
        from { transform: translateX(100%); opacity: 0; }
        to { transform: translateX(0); opacity: 1; }
    }
    
    @@keyframes slide-in-up {
        from { transform: translateY(100%); opacity: 0; }
        to { transform: translateY(0); opacity: 1; }
    }
    
    @@keyframes slide-in-down {
        from { transform: translateY(-100%); opacity: 0; }
        to { transform: translateY(0); opacity: 1; }
    }
    
    .animate-slide-in-left { animation: slide-in-left 0.6s ease-out; }
    .animate-slide-in-right { animation: slide-in-right 0.6s ease-out; }
    .animate-slide-in-up { animation: slide-in-up 0.6s ease-out; }
    .animate-slide-in-down { animation: slide-in-down 0.6s ease-out; }

    /* Rotate & Flip */
    @@keyframes rotate-in {
        from { transform: rotate(-360deg) scale(0); opacity: 0; }
        to { transform: rotate(0) scale(1); opacity: 1; }
    }
    
    @@keyframes flip-in-x {
        from { transform: perspective(400px) rotateX(-90deg); opacity: 0; }
        to { transform: perspective(400px) rotateX(0); opacity: 1; }
    }
    
    @@keyframes flip-in-y {
        from { transform: perspective(400px) rotateY(-90deg); opacity: 0; }
        to { transform: perspective(400px) rotateY(0); opacity: 1; }
    }
    
    @@keyframes spin-slow {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
    }
    
    .animate-rotate-in { animation: rotate-in 0.8s ease-out; }
    .animate-flip-in-x { animation: flip-in-x 0.8s ease-out; }
    .animate-flip-in-y { animation: flip-in-y 0.8s ease-out; }
    .animate-spin-slow { animation: spin-slow 3s linear infinite; }

    /* Scale & Zoom */
    @@keyframes zoom-in {
        from { transform: scale(0); opacity: 0; }
        to { transform: scale(1); opacity: 1; }
    }
    
    @@keyframes scale-up {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.1); }
    }
    
    @@keyframes pulse-scale {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.15); }
    }
    
    .animate-zoom-in { animation: zoom-in 0.5s ease-out; }
    .animate-scale-up { animation: scale-up 2s ease-in-out infinite; }
    .animate-pulse-scale:hover { animation: pulse-scale 0.6s ease-in-out; }

    /* Shake & Wobble */
    @@keyframes shake {
        0%, 100% { transform: translateX(0); }
        10%, 30%, 50%, 70%, 90% { transform: translateX(-10px); }
        20%, 40%, 60%, 80% { transform: translateX(10px); }
    }
    
    @@keyframes wobble {
        0%, 100% { transform: translateX(0) rotate(0deg); }
        15% { transform: translateX(-25px) rotate(-5deg); }
        30% { transform: translateX(20px) rotate(3deg); }
        45% { transform: translateX(-15px) rotate(-3deg); }
        60% { transform: translateX(10px) rotate(2deg); }
        75% { transform: translateX(-5px) rotate(-1deg); }
    }
    
    @@keyframes swing {
        20% { transform: rotate(15deg); }
        40% { transform: rotate(-10deg); }
        60% { transform: rotate(5deg); }
        80% { transform: rotate(-5deg); }
        100% { transform: rotate(0deg); }
    }
    
    @@keyframes jello {
        0%, 100% { transform: skewX(0deg) skewY(0deg); }
        30% { transform: skewX(25deg) skewY(25deg); }
        40% { transform: skewX(-20deg) skewY(-20deg); }
        50% { transform: skewX(15deg) skewY(15deg); }
        65% { transform: skewX(-10deg) skewY(-10deg); }
        75% { transform: skewX(5deg) skewY(5deg); }
    }
    
    .animate-shake { animation: shake 0.8s ease-in-out infinite; }
    .animate-wobble { animation: wobble 1s ease-in-out infinite; }
    .animate-swing { animation: swing 1s ease-in-out infinite; }
    .animate-jello { animation: jello 1s ease-in-out infinite; }

    /* Glow & Pulse */
    @@keyframes glow {
        0%, 100% { box-shadow: 0 0 5px currentColor; }
        50% { box-shadow: 0 0 20px currentColor, 0 0 30px currentColor; }
    }
    
    @@keyframes pulse-glow {
        0%, 100% { box-shadow: 0 0 10px currentColor; transform: scale(1); }
        50% { box-shadow: 0 0 30px currentColor, 0 0 40px currentColor; transform: scale(1.05); }
    }
    
    @@keyframes progress {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
    }
    
    @@keyframes rainbow {
        0% { filter: hue-rotate(0deg); }
        100% { filter: hue-rotate(360deg); }
    }
    
    .animate-glow { animation: glow 2s ease-in-out infinite; }
    .animate-pulse-glow { animation: pulse-glow 2s ease-in-out infinite; }
    .animate-progress { animation: progress 2s linear infinite; }
    .animate-rainbow { animation: rainbow 3s linear infinite; }

    /* Text Animations */
    @@keyframes text-focus-in {
        0% { filter: blur(12px); opacity: 0; }
        100% { filter: blur(0); opacity: 1; }
    }
    
    @@keyframes text-blur-out {
        0% { filter: blur(0); }
        100% { filter: blur(12px); opacity: 0.5; }
    }
    
    @@keyframes typing {
        from { width: 0; }
        to { width: 100%; }
    }
    
    @@keyframes gradient {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }
    
    .animate-text-focus-in { animation: text-focus-in 1s ease-out; }
    .animate-text-blur-out { animation: text-blur-out 2s ease-out infinite alternate; }
    .animate-typing { 
        overflow: hidden;
        white-space: nowrap;
        animation: typing 3s steps(20) infinite;
    }
    .animate-gradient { 
        background-size: 200% 200%;
        animation: gradient 3s ease infinite;
    }

    /* Complex Animations */
    @@keyframes float {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-20px); }
    }
    
    @@keyframes tilt {
        0%, 100% { transform: perspective(1000px) rotateY(-5deg); }
        50% { transform: perspective(1000px) rotateY(5deg); }
    }
    
    .animate-float { animation: float 3s ease-in-out infinite; }
    .animate-tilt { animation: tilt 3s ease-in-out infinite; }

    /* Heartbeat & Pulse */
    @@keyframes heartbeat {
        0%, 100% { transform: scale(1); }
        10%, 30% { transform: scale(0.9); }
        20%, 40% { transform: scale(1.1); }
    }
    
    @@keyframes pulse-ring {
        0% { box-shadow: 0 0 0 0 currentColor; }
        100% { box-shadow: 0 0 0 20px rgba(0, 0, 0, 0); }
    }
    
    @@keyframes ping-badge {
        75%, 100% { transform: scale(1.2); opacity: 0; }
    }
    
    .animate-heartbeat { animation: heartbeat 1.3s ease-in-out infinite; }
    .animate-pulse-ring { animation: pulse-ring 1.5s cubic-bezier(0, 0, 0.2, 1) infinite; }
    .animate-ping-badge { animation: ping-badge 1s cubic-bezier(0, 0, 0.2, 1) infinite; }

    /* Elastic & Rubber Band */
    @@keyframes elastic {
        0%, 100% { transform: scale(1); }
        30% { transform: scale(1.25) rotate(3deg); }
        40% { transform: scale(0.75) rotate(-3deg); }
        50% { transform: scale(1.15) rotate(3deg); }
        65% { transform: scale(0.95) rotate(-3deg); }
        75% { transform: scale(1.05) rotate(2deg); }
    }
    
    @@keyframes rubber-band {
        0%, 100% { transform: scale(1); }
        30% { transform: scaleX(1.25) scaleY(0.75); }
        40% { transform: scaleX(0.75) scaleY(1.25); }
        50% { transform: scaleX(1.15) scaleY(0.85); }
        65% { transform: scaleX(0.95) scaleY(1.05); }
        75% { transform: scaleX(1.05) scaleY(0.95); }
    }
    
    @@keyframes bounce-elastic {
        0%, 100% { transform: translateY(0); }
        25% { transform: translateY(-30px); }
        50% { transform: translateY(0); }
        75% { transform: translateY(-15px); }
    }
    
    .animate-elastic:hover { animation: elastic 1s ease; }
    .animate-rubber-band:hover { animation: rubber-band 1s ease; }
    .animate-bounce-elastic { animation: bounce-elastic 2s ease-in-out infinite; }

    /* Roll & Rotate */
    @@keyframes roll-in {
        0% { transform: translateX(-100%) rotate(-360deg); opacity: 0; }
        100% { transform: translateX(0) rotate(0); opacity: 1; }
    }
    
    @@keyframes rotate-scale {
        0%, 100% { transform: rotate(0) scale(1); }
        50% { transform: rotate(180deg) scale(1.2); }
    }
    
    @@keyframes flip-horizontal {
        0%, 100% { transform: rotateY(0); }
        50% { transform: rotateY(180deg); }
    }
    
    @@keyframes flip-vertical {
        0%, 100% { transform: rotateX(0); }
        50% { transform: rotateX(180deg); }
    }
    
    .animate-roll-in { animation: roll-in 1s ease-out; }
    .animate-rotate-scale { animation: rotate-scale 3s ease-in-out infinite; }
    .animate-flip-horizontal { animation: flip-horizontal 2s ease-in-out infinite; }
    .animate-flip-vertical { animation: flip-vertical 2s ease-in-out infinite; }

    /* Attention Seekers */
    @@keyframes flash {
        0%, 50%, 100% { opacity: 1; }
        25%, 75% { opacity: 0; }
    }
    
    @@keyframes tada {
        0%, 100% { transform: scale(1) rotate(0); }
        10%, 20% { transform: scale(0.9) rotate(-3deg); }
        30%, 50%, 70%, 90% { transform: scale(1.1) rotate(3deg); }
        40%, 60%, 80% { transform: scale(1.1) rotate(-3deg); }
    }
    
    @@keyframes head-shake {
        0%, 50% { transform: translateX(0); }
        6.5% { transform: translateX(-6px) rotateY(-9deg); }
        18.5% { transform: translateX(5px) rotateY(7deg); }
        31.5% { transform: translateX(-3px) rotateY(-5deg); }
        43.5% { transform: translateX(2px) rotateY(3deg); }
    }
    
    @@keyframes bounce-in-out {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.2); }
    }
    
    .animate-flash { animation: flash 2s ease-in-out infinite; }
    .animate-tada:hover { animation: tada 1s ease; }
    .animate-head-shake { animation: head-shake 2s ease-in-out infinite; }
    .animate-bounce-in-out { animation: bounce-in-out 1s ease-in-out infinite; }

    /* Background Animations */
    @@keyframes bg-pan-left {
        0% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }
    
    @@keyframes bg-pan-right {
        0% { background-position: 0% 50%; }
        100% { background-position: 100% 50%; }
    }
    
    @@keyframes kenburns {
        0% { transform: scale(1) translate(0, 0); }
        100% { transform: scale(1.2) translate(20px, 15px); }
    }
    
    .animate-bg-pan-left {
        background: linear-gradient(90deg, hsl(var(--p)) 0%, hsl(var(--s)) 100%);
        background-size: 200% 100%;
        animation: bg-pan-left 8s ease infinite alternate;
    }
    
    .animate-bg-pan-right {
        background: linear-gradient(90deg, hsl(var(--s)) 0%, hsl(var(--a)) 100%);
        background-size: 200% 100%;
        animation: bg-pan-right 8s ease infinite alternate;
    }
    
    .animate-kenburns {
        background: linear-gradient(135deg, hsl(var(--p)) 0%, hsl(var(--a)) 100%);
        animation: kenburns 10s ease infinite alternate;
    }

    /* Tracking & Spacing */
    @@keyframes tracking-in {
        0% { letter-spacing: -0.5em; opacity: 0; }
        40% { opacity: 0.6; }
        100% { letter-spacing: normal; opacity: 1; }
    }
    
    @@keyframes tracking-out {
        0% { letter-spacing: normal; opacity: 1; }
        60% { opacity: 0.6; }
        100% { letter-spacing: 0.5em; opacity: 0; }
    }
    
    @@keyframes text-pop-up {
        0% { transform: translateY(0); }
        50% { transform: translateY(-20px); }
        100% { transform: translateY(0); }
    }
    
    .animate-tracking-in { animation: tracking-in 1s ease-out; }
    .animate-tracking-out { animation: tracking-out 2s ease-out infinite alternate; }
    .animate-text-pop-up { animation: text-pop-up 0.5s ease-in-out infinite; }

    /* Shadow Effects */
    @@keyframes shadow-drop {
        0% { box-shadow: 0 0 0 0 rgba(0, 0, 0, 0); }
        100% { box-shadow: 0 10px 20px 0 rgba(0, 0, 0, 0.35); }
    }
    
    @@keyframes shadow-inset {
        0% { box-shadow: inset 0 0 0 0 rgba(0, 0, 0, 0); }
        100% { box-shadow: inset 0 0 20px 0 rgba(0, 0, 0, 0.35); }
    }
    
    @@keyframes shadow-pulse {
        0%, 100% { box-shadow: 0 0 0 0 currentColor; }
        50% { box-shadow: 0 0 20px 10px currentColor; }
    }
    
    .animate-shadow-drop { animation: shadow-drop 1s ease-out forwards; }
    .animate-shadow-inset { animation: shadow-inset 1s ease-out forwards; }
    .animate-shadow-pulse { animation: shadow-pulse 2s ease-in-out infinite; }

    /* Back Entrance/Exit */
    @@keyframes back-in-up {
        0% { transform: translateY(1200px) scale(0.7); opacity: 0.7; }
        80% { transform: translateY(0) scale(0.7); opacity: 0.7; }
        100% { transform: scale(1); opacity: 1; }
    }
    
    @@keyframes back-in-down {
        0% { transform: translateY(-1200px) scale(0.7); opacity: 0.7; }
        80% { transform: translateY(0) scale(0.7); opacity: 0.7; }
        100% { transform: scale(1); opacity: 1; }
    }
    
    @@keyframes back-in-left {
        0% { transform: translateX(-2000px) scale(0.7); opacity: 0.7; }
        80% { transform: translateX(0) scale(0.7); opacity: 0.7; }
        100% { transform: scale(1); opacity: 1; }
    }
    
    @@keyframes back-in-right {
        0% { transform: translateX(2000px) scale(0.7); opacity: 0.7; }
        80% { transform: translateX(0) scale(0.7); opacity: 0.7; }
        100% { transform: scale(1); opacity: 1; }
    }
    
    .animate-back-in-up { animation: back-in-up 1s ease-out; }
    .animate-back-in-down { animation: back-in-down 1s ease-out; }
    .animate-back-in-left { animation: back-in-left 1s ease-out; }
    .animate-back-in-right { animation: back-in-right 1s ease-out; }

    /* Light Speed */
    @@keyframes light-speed-in-right {
        0% { transform: translateX(100%) skewX(-30deg); opacity: 0; }
        60% { transform: skewX(20deg); opacity: 1; }
        80% { transform: skewX(-5deg); }
        100% { transform: translateX(0) skewX(0); }
    }
    
    @@keyframes light-speed-in-left {
        0% { transform: translateX(-100%) skewX(30deg); opacity: 0; }
        60% { transform: skewX(-20deg); opacity: 1; }
        80% { transform: skewX(5deg); }
        100% { transform: translateX(0) skewX(0); }
    }
    
    .animate-light-speed-in-right { animation: light-speed-in-right 1s ease-out; }
    .animate-light-speed-in-left { animation: light-speed-in-left 1s ease-out; }

    /* Hinge & Roll Out */
    @@keyframes hinge {
        0% { transform-origin: top left; }
        20%, 60% { transform: rotate(80deg); transform-origin: top left; }
        40%, 80% { transform: rotate(60deg); transform-origin: top left; opacity: 1; }
        100% { transform: translateY(700px); opacity: 0; }
    }
    
    @@keyframes roll-out {
        0% { opacity: 1; }
        100% { transform: translateX(100%) rotate(360deg); opacity: 0; }
    }
    
    .animate-hinge { animation: hinge 2s ease-in-out; }
    .animate-roll-out { animation: roll-out 1s ease-in-out; }

    /* Zoom Variations */
    @@keyframes zoom-in-up {
        0% { transform: scale(0.1) translateY(1000px); opacity: 0; }
        60% { opacity: 1; }
        100% { transform: scale(1) translateY(0); }
    }
    
    @@keyframes zoom-in-down {
        0% { transform: scale(0.1) translateY(-1000px); opacity: 0; }
        60% { opacity: 1; }
        100% { transform: scale(1) translateY(0); }
    }
    
    @@keyframes zoom-in-left {
        0% { transform: scale(0.1) translateX(-1000px); opacity: 0; }
        60% { opacity: 1; }
        100% { transform: scale(1) translateX(0); }
    }
    
    @@keyframes zoom-in-right {
        0% { transform: scale(0.1) translateX(1000px); opacity: 0; }
        60% { opacity: 1; }
        100% { transform: scale(1) translateX(0); }
    }
    
    .animate-zoom-in-up { animation: zoom-in-up 1s ease-out; }
    .animate-zoom-in-down { animation: zoom-in-down 1s ease-out; }
    .animate-zoom-in-left { animation: zoom-in-left 1s ease-out; }
    .animate-zoom-in-right { animation: zoom-in-right 1s ease-out; }

    /* Specials */
    @@keyframes jack-in-box {
        0% { transform: scale(0.1) rotate(30deg); transform-origin: center bottom; opacity: 0; }
        50% { transform: rotate(-10deg); }
        70% { transform: rotate(3deg); }
        100% { transform: scale(1) rotate(0); opacity: 1; }
    }
    
    @@keyframes flip {
        0% { transform: perspective(400px) rotateY(0); }
        40% { transform: perspective(400px) rotateY(170deg); }
        50% { transform: perspective(400px) rotateY(190deg) scale(1); }
        80% { transform: perspective(400px) rotateY(360deg) scale(0.95); }
        100% { transform: perspective(400px) scale(1); }
    }
    
    @@keyframes zoom-out-in {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(0.5); }
    }
    
    .animate-jack-in-box:hover { animation: jack-in-box 1s ease; }
    .animate-flip { animation: flip 1s ease-in-out; }
    .animate-zoom-out-in:hover { animation: zoom-out-in 0.6s ease; }

    /* 3D Card Flip */
    .flip-card {
        perspective: 1000px;
        width: 16rem;
        height: 20rem;
    }
    
    .flip-card-inner {
        position: relative;
        width: 100%;
        height: 100%;
        transition: transform 0.8s;
        transform-style: preserve-3d;
    }
    
    .flip-card:hover .flip-card-inner {
        transform: rotateY(180deg);
    }
    
    .flip-card-front,
    .flip-card-back {
        position: absolute;
        width: 100%;
        height: 100%;
        backface-visibility: hidden;
        -webkit-backface-visibility: hidden;
    }
    
    .flip-card-back {
        transform: rotateY(180deg);
    }
</style>
