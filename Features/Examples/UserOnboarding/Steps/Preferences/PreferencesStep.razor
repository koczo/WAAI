<div class="max-w-md mx-auto space-y-4">
    <div class="prose prose-lg">
        <h2>Preferences</h2>
    </div>
    <div class="form-control w-full">
        <label class="label">
            <span class="label-text">Language <span class="text-error">*</span></span>
        </label>
        <InputSelect @bind-Value="Model.Language" class="select select-bordered w-full">
            <option value="en">English</option>
            <option value="pl">Polish</option>
            <option value="de">German</option>
        </InputSelect>
    </div>
    <div class="form-control w-full">
        <label class="label">
            <span class="label-text">Theme <span class="text-error">*</span></span>
        </label>
        <div class="grid grid-cols-3 gap-4">
            <button type="button" @onclick="SetThemeLight" class="btn @(Model.Theme == "light" ? "btn-primary" : "btn-outline")">
                <div class="text-3xl">☀️</div>
                Light
            </button>
            <button type="button" @onclick="SetThemeDark" class="btn @(Model.Theme == "dark" ? "btn-primary" : "btn-outline")">
                <div class="text-3xl">🌙</div>
                Dark
            </button>
            <button type="button" @onclick="SetThemeAuto" class="btn @(Model.Theme == "auto" ? "btn-primary" : "btn-outline")">
                <div class="text-3xl">⚙️</div>
                Auto
            </button>
        </div>
    </div>
    <div class="form-control w-full">
        <label class="label cursor-pointer justify-start gap-2">
            <InputCheckbox @bind-Value="Model.Newsletter" class="checkbox checkbox-primary"/>
            <span class="label-text">Subscribe to newsletter</span>
        </label>
    </div>
</div>
